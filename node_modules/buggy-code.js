// buggy-code.js
function calculateTotal(items) {
  let total = 0
  for (i = 0; i < items.length; i++) { // Ошибка: нет объявления переменной
    total += items[i].price
  }
  return total
  console.log('Расчёт завершён') // Бессмысленная строка после return
}

const cart = [{ price: 10 }, { price: 20 }]
console.log('Итого:', calculatTotal(cart)) // Опечатка в имени функции